const BASE_URL = "https://event-nation-backend.onrender.com";

export const API_BASE = `${BASE_URL}/api`;

export const API = {
  AUTH: `${API_BASE}/auth`,
  EVENTS: `${API_BASE}/events`,
  BOOKINGS: `${API_BASE}/bookings`,
  TICKETS: `${API_BASE}/tickets`,
  ADMIN: `${API_BASE}/admin`,
  PACKAGES: `${API_BASE}/packages`,
};
